# Duffel TEST-mode token only (duffel_test_...). Never a live token.
DUFFEL_ACCESS_TOKEN=
# At least 32 random characters. Sessions are encrypted with this.
SESSION_SECRET=
# duffel | fixture   (fixture = deterministic local data, clearly badged in the UI)
TRIPRESCUE_PROVIDER=fixture
